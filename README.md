YEOUN PLAYLIST

This project now uses the YouTube IFrame Player API for both single videos and playlist URLs.

- No YouTube Data API key is required.
- Paste either a YouTube video URL or a playlist URL.
- Playlist playback, previous/next, and title fetching work without exposing an API key in the frontend.
